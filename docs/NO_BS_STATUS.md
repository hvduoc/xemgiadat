# NO BS STATUS – Repo Hygiene & Verification (2026-01-24)

## What Was Wrong
- Repo bloat risk: `node_modules/` and compiled V2 assets tracked.
- Sourcemaps (`*.map`) and large artifacts increase repo size and risk accidental commits.

## What Changed
- Step B (Repo Hygiene):
  - Tightened `.gitignore` to exclude `node_modules/`, `dist/`, `.vite/`, `*.log`, `*.tmp`, optional `*.map`, `dev-server.log`.
  - Untracked `node_modules/` from Git index (local files preserved).
- Step C (Artifact Policy):
  - Adopted Source-only policy for V2.
  - Untracked `public/v2/assets/` (generated by Vite per [vite.config.js](../vite.config.js)).
  - Documented policy in [docs/ARTIFACT_POLICY.md](ARTIFACT_POLICY.md).
- Step D (PowerShell Equivalents):
  - Updated grep/head usages to `Select-String` / `Select-Object -First` in [docs/POWERSHELL_SNIPPETS.md](POWERSHELL_SNIPPETS.md).

## Production Verification (Next Deploy)
- Legacy `/`:
  - Expect Leaflet map and `[IDENTITY] LEGACY` in console.
- V2 `/v2/`:
  - Hard refresh 5x → V2 shell consistently.
  - Network: `/v2/assets/*` return `200` with `Content-Type: text/javascript`.
  - Tiles: `/tiles/metadata.json` returns `200` (no 301/302).
  - Console: `[VERIFY SW] Active version: 2026-01-24-routing-fix`.

## Evidence (Current Deploy)
See [docs/PROD_SMOKE_20260124.md](PROD_SMOKE_20260124.md) for HEAD checks and observations.

## Candidates (Keep for Now)
- `public/assets/*` – referenced by legacy [public/index.html](../public/index.html); audit later.
- `*.map` – now ignored to prevent future commits; existing tracked maps were untracked under V2.
