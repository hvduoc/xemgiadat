# FILE PREPROCESSING TOOLS - REQUIREMENTS
# Install with: pip install -r requirements.txt

# Core processing libraries
Pillow>=10.0.0              # Image processing and optimization
geopandas>=0.14.0           # Geospatial data processing
fiona>=1.9.0                # Geospatial file I/O
shapely>=2.0.0              # Geometric operations
pyproj>=3.6.0               # Coordinate system transformations

# DWG/DXF processing
ezdxf>=1.1.0                # DXF file parsing and processing

# Optional but recommended
requests>=2.31.0            # HTTP requests for API integration
tqdm>=4.66.0                # Progress bars for batch processing
pandas>=2.1.0               # Data manipulation and analysis

# Development and testing
pytest>=7.4.0              # Testing framework
black>=23.0.0               # Code formatting
flake8>=6.0.0               # Code linting

# Platform-specific tools (install separately)
# Windows: ODA File Converter (https://www.opendesign.com/guestfiles)
# Linux: teigha2dwg, librecad
# macOS: Install via Homebrew